/**
 * #930: the sidebar lives at `components/AgentSidebar.tsx` (App shell rail).
 * This folder retains only shared avatar internals consumed by AgentChat —
 * the diverged duplicate sidebar was deleted.
 */
export { AgentAvatar } from './AgentAvatar'
export { RobotAvatar } from './RobotAvatar'

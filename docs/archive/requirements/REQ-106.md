# REQ-106 — Bee brand mark: SVG favicon + multi-size app icons

**Status:** this PR — #470

## Intent

Open Swarm’s browser tab / PWA / future installable app use a recognisable
**bee** mark derived from the README art, not a generic default.

## Success

1. Master SVGs in `assets/brand/`: colour (yellow/black, 16px-readable,
   silhouette-first), mono (`currentColor`), mono-on-dark (white).
2. Checked-in rasters: `favicon.ico` (16/32/48), `favicon-16.png`,
   `favicon-32.png`, Apple/PWA at least 180 / 192 / 512. Colour wired in the
   tab/manifest; mono sizes documented in `assets/brand/README.md`.
3. SPA + Django static: tab favicon, `apple-touch-icon`, and manifest point at
   the new assets. `assets/images/favicon.ico` replaced with the colour ICO.
4. README hero JPG stays; one-line note that the mark lives under
   `assets/brand/`.
5. Same masters are the source for a later native / Pinokio icon. This change
   delivers the asset kit (Pinokio `icon` now points at `bee-mark.svg`).
6. Project-owned; no third-party stock bee.
7. Head references resolve (no 404). 16px tab still reads as a bee.

## Constraints

- Parked design/asset work. No Neon. No secrets. GitHub-only until the
  `:8001` deploy wave.
- Keep the mark simple — photo-real banner bee will mud at favicon size.
- DaisyUI chrome colours unrelated except mono must work on dark rail
  `#111111`.
- One cloud. PR says `Fixes` this issue.

## Owner

- CoS transcribes
- cloud implements
- engineer GitHub-merge after skeptic
- live preview `198.51.100.30:8001` guest dirty only
